{
  "name": "{{serviceName}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/service.js",
  "scripts": {
    "dev": "node --watch --import tsx src/service.js --config ./xenosis.config.json",
    "start": "node src/service.js --config ./xenosis.config.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@xenosisorg/xenosis-core": "^0.0.12",
    "awilix": "^12.0.5",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@xenosisorg/xenosis-testing": "^0.0.4",
    "supertest": "^7.0.0",
    "tsx": "^4.19.3",
    "vitest": "^4.0.0"
  }
}
